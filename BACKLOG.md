# BACKLOG

รายการที่เจอตอนทำงานแต่อยู่นอกขอบเขตของงานนั้น จดไว้ทำภายหลัง

---

## ผลการพิสูจน์ความเสี่ยง

### R1 — คุณสมบัติ "ไฟล์เดียว" (สเปก §14)

สเปกเตือนว่า `vite-plugin-singlefile` + Web Worker + MapLibre อาจเข้ากันไม่ได้
**ทดลองจริงแล้ว ผลออกมาละเอียดกว่าสองทางเลือกที่สเปกคาดไว้** — คุณสมบัตินี้ไม่ได้หายไป

| ทางเลือก | ได้ไฟล์ | ผลการทดสอบ |
|---|---|---|
| **ก. `?worker` (ที่ใช้อยู่)** เสิร์ฟ `dist-single/` ผ่าน http | `index.html` 1.17 MB + `worker-*.js` 8.9 kB = **2 ไฟล์** | smoke test ผ่าน 17/17 · เวิร์กเกอร์ทำงานปกติ |
| **ข. `?worker` เปิดจาก `file://` ตรงๆ** | ใช้แค่ `index.html` ไฟล์เดียว | **ทำงานได้** แผนที่ขึ้น ไทล์โหลด 30/30 · คำนวณได้ผ่านทางถอยบนเธรดหลัก พีค 104.25 µg/m³ · 0 console error · เวิร์กเกอร์โหลดไม่ได้ตามคาด (file:// ห้าม) แต่ถอยถูกทาง |
| **ค. `?worker&inline`** | `index.html` 1.18 MB = **ไฟล์เดียวจริง** | build ผ่าน · ต้นทุน: โตขึ้น 9 kB และ **worker ถูก inline ในทุก build รวมตัวที่โฮสต์** จึงเสียการแคชแยกของ chunk 9 kB |

**ที่เลือกไว้ตอนนี้คือ ก.** เพราะเป้าหมายหลักคือแอปที่โฮสต์และให้คนฝัง
การมี worker เป็น chunk แยกที่แคชได้เองเหมาะกับปลายทางนั้นกว่า
และโหมดไฟล์เดียวก็ยังใช้งานได้อยู่ทั้งแบบ 2 ไฟล์และแบบไฟล์เดียวผ่านทางถอย

**ถ้าอยากได้ไฟล์เดียวจริงเป็นค่าปริยาย** เปลี่ยนบรรทัดเดียวใน `app/src/app.js`

```js
import EngineWorker from 'mokhwan-engine/worker?worker&inline';
```

เป็นการตัดสินใจของเจ้าของงานว่าจะให้ปลายทางไหนได้เปรียบ ไม่ใช่เรื่องที่ทำไม่ได้

---

## บั๊กที่รู้อยู่ ยังไม่แก้

### 1. เวิร์กเกอร์ error ระหว่างมีคำขอค้าง → UI ค้างเงียบ

`worker.onerror` ตั้ง `worker = null` แต่**ไม่เรียก `pendingResolve`** promise ของคำขอที่ค้างอยู่
จึงไม่ settle ตลอดกาล `runSim()` ที่ `await` อยู่ไม่ไปต่อ `S.computing` ค้างเป็น `true`
โดยไม่มีข้อความบอกผู้ใช้ เกิดได้จริงถ้า `run()` ในเวิร์กเกอร์โยน exception

**มีมาแต่เดิมเหมือนกันทุกบรรทัด** ทั้งในไฟล์ตั้งต้นและหลังย้าย งานรื้อโครงไม่ได้ทำให้แย่ลง
พบจาก code review ของ Task 4 ที่ไล่เส้นทาง error ทีละกรณี

**วิธีแก้** ให้ `onerror` เรียก `pendingResolve` ด้วยการคำนวณบนเธรดหลักแทน หรือ reject
แล้วให้ `runSim` จับแล้วแสดงข้อความไทย

### 2. `pendingResolve` เป็นช่องเดียว

กดรันซ้อนกันจะเขียนทับ resolver ตัวเก่า promise แรกไม่ settle ตลอดกาล
ไม่ใช่บั๊กความถูกต้องเพราะ `reqId` กันลำดับผลไว้แล้ว แต่เป็น promise ที่ค้าง
**แก้ด้วย `Map<reqId, resolver>`** — เรื่องเดียวกับข้อ 1 แก้พร้อมกันได้

### 3. เลขค่าตกสะสมแห้งถูกเขียนซ้ำสองที่

`gaussian.ts:13` ประกาศ `var VD = 0.004` แล้วใช้ที่ `:44`
ส่วน `puff.ts:104` เขียน `Math.exp(-0.004*tt/hef)` โดยฝังเลขตรงๆ ไม่ผ่าน `VD`
(เหมือนไฟล์ตั้งต้นบรรทัด 782 ทุกตัวอักษร) **แก้ค่าที่เดียวแล้วอีกที่ไม่ตาม**

**บันทึกเดิมของข้อนี้เขียนผิด** เคยเขียนว่า "`runPuff` อาจไม่มีการตกสะสมแห้ง"
โดยอ้างว่า `VD` ไม่ปรากฏใน `puff.ts` — จริงเรื่อง**ชื่อตัวแปร** แต่ผิดเรื่อง**พฤติกรรม**
`runPuff` มีการตกสะสมแห้งครบ code review รอบสุดท้ายจับได้
ถ้าปล่อยไว้ ก้าว 5 จะอ่านบันทึกนี้เป็นเช็กลิสต์แล้วไปหากลไกที่มีอยู่แล้ว

**วิธีแก้** ย้าย `VD` ไปไว้ที่เดียวที่ทั้งสองโมดูล import ได้ แล้วให้ `puff.ts` ใช้ตัวนั้น
เป็นการแก้ที่เปลี่ยนพฤติกรรมเป็นศูนย์ แต่ต้องรัน golden test คุม

### 4. ~~โหมด puff ให้ความเข้มข้นระดับพื้นแทบเป็นศูนย์~~ — **แก้แล้ว (ก้าว 5 ข้อ 1)**

**ต้นเหตุ** Briggs `sigmas(x)` นิยามด้วยระยะทางท้ายลม โมเดล puff ใช้ `pf.d` = ความยาว
เส้นทางที่ puff เดินเอง พอ puff ชะงักที่ก้นแอ่ง (ลมหลักถูก shelter เหลือ 0.32 เท่ากับลมไหล
ลงลาดที่ดันสวน วัดได้ cos มุม −1.00 ลมสุทธิ 0.00 · 56/60 puff ต่ำกว่า clamp) `d` หยุดโต
σz แช่ที่ ~9.6 ม. แผ่นควันคุกรุ่นค้างที่ 40 ม. เหนือพื้น `exp(−½(40/9.6)²) ≈ 1.7×10⁻⁴`
ฆ่าค่าที่พื้นหมด — ควันกระจุกในแนวราบถูก แต่ไม่เคยกระจายลงถึงพื้นในแนวดิ่ง

**แก้** `puff.ts`: `σ = sigmas(max(pf.d, max(ws, SIGMA_WS_FLOOR)·t, 12))` ใช้ระยะเทียบเท่า
ลมแวดล้อมเป็นขั้นต่ำ พร้อมพื้นความปั่นป่วน `SIGMA_WS_FLOOR = 1.0 m/s`
(ไม่มีพื้นนี้ ที่ ws 0.4 ได้พีค 0.14 = อาการเดิม · มีแล้วได้ 53.8 · คุมด้วย `puffTerrainCalm`) · ผล `puffTerrain` พีค 0.139 → **1,069** (×10.3 ของพื้นราบ 103.8 · พีคอยู่ห่าง
จุดเผา 136 ม. = กระจุกในแอ่งจริง) · `puffFlat` พีค/รายชั่วโมง/จุดรับ **เท่าเดิมทุกบิต**
แต่ผลรวมกริดเลื่อน ~5×10⁻⁹ จากลำดับทศนิยมลอยตัวของ `max()` จึงบันทึก golden ใหม่ทั้งสองเคส

**สมมติฐานใน HANDOFF ที่ตกไป** ข้อ "ตรวจว่า `pf.z` ตั้งต้นเป็น terrain+plumeRise" — โค้ดถูกอยู่แล้ว
(บรรทัด `z: sample(Z,…) + S0.H`) · cutoff `1e-4` ไม่ใช่สาเหตุ (norm ของเฟสคุกรุ่นตกไปถึง 0.00
ก่อนถึง gate)

**เกณฑ์รับงานอยู่ใน `test/terrain.test.ts`** — assertion เดิม "ข้ามสันเขา" เขียนหน่วยปนกัน
(ลบ 1800 ม. ออกจากผลบวก `x+y` ซึ่งไม่ใช่ระยะตั้งฉาก) และจุดที่ได้อยู่ไกลกว่าที่ควันเดินถึงใน
1 ชม. จึงผ่านฟรี · แทนด้วยการวัด **มวลที่พื้นบนเซลล์ที่ภูมิประเทศสูงเกิน 600 ม.** ซึ่งเป็นวิธี
เดียวกับที่ใช้ตรวจรับด้วย DEM จริง และเพิ่ม assertion ว่าพีคต้องอยู่ใกล้จุดเผากว่าพื้นราบ 3 เท่า

**สมมติฐานเชิงแบบจำลองที่เจ้าของงานควรทบทวน** `SIGMA_WS_FLOOR = 1.0 m/s` คือการบอกว่า
"ความปั่นป่วนไม่หยุดตามลมเฉลี่ย" ด้วยตัวเลขที่เลือกเอง ไม่ได้มาจากการวัด · เส้นโค้ง Briggs
ฟิตจากพลูมที่ถูกลมพัดไป การใช้ `ws·t` จึงเป็นตัวแทนเชิงวิศวกรรม ไม่ใช่การคืนนิยาม
ตัวเลขในแอ่งลมนิ่งควรใช้เปรียบเทียบทางเลือก ไม่ใช่อ่านเป็นค่าสัมบูรณ์

### 5. เกณฑ์ข้อ 1 ของ HANDOFF (puff ≈ Gaussian ±25% ที่ 1/3/8 กม.) ยังไม่ผ่าน — **ต้องตัดสินใจ**

HANDOFF เขียนว่า "โหมดพื้นราบทำงานถูกต้อง ให้ผลตรงกับ Gaussian" แต่เทียบแค่**พีค** (ซึ่งผ่านจริง
ต่างไม่เกิน 50%) ไม่เคยวัดที่ 1/3/8 กม. พอเขียนเทสต์ตามเกณฑ์ตรงๆ (`test/puff-vs-gauss.test.ts`)
แดง 3 ใน 3 ระยะ ทำเป็น `it.fails` ไว้ — ถ้าวันไหนผ่านขึ้นมา เทสต์จะแดงเพื่อบอกให้เลื่อนขั้น

| ระยะ | puff | gauss | ต่าง | สถานะ |
|---|---|---|---|---|
| 8 กม. | 0.00 | 53.66 | −100% | **พิสูจน์แล้วว่าเป็นเรื่องนิยาม** ลม 1.3 m/s × 3,600 s = 4,680 ม. puff ไปไม่ถึง Gaussian เป็น steady-state puff เป็น transient ไม่มีโมเดล puff ที่ถูกต้องตัวไหนผ่านเกณฑ์นี้ในรอบ 1 ชม. |
| 3 กม. | 59.79 | 94.30 | −37% | ยังไม่พิสูจน์ · สงสัยค่าเฉลี่ยรายชั่วโมงถูกเจือจางด้วยช่วงที่ควันยังเดินไม่ถึง (~2,300 s) |
| 1 กม. | 7.69 | 4.39 | +75% | ยังไม่พิสูจน์ · สงสัยปล่อย puff ทุก 60 s = ห่าง 78 ม. ใกล้เคียง σy ใกล้จุดเผา (~50 ม.) จึงเป็นก้อนๆ |

**เจ้าของงานเลือก (ก) แล้ว** — `test/puff-vs-gauss.test.ts` เทียบเฉพาะระยะที่ควันถึง ส่วน 1/3 กม.
ยังเป็น `it.fails` รอหาสาเหตุ · **ข้อสังเกตเพิ่ม (ก้าว D)** ฉากเชียงใหม่จริง มวลรวมระดับพื้นของ puff
เหลือ 20% ของ gauss (7,377 vs 36,107) ยังไม่มีการตรวจ mass balance ว่าส่วนที่หายไปลอยอยู่
เหนือ σz หรือหลุดออกโดเมน — ควรทำ diagnostic รวมมวลทุกชั้นก่อนสรุปว่าถูกหรือผิด
(ข)/(ค) ยังเป็นทางเลือกสำหรับส่วนต่าง 1/3 กม. · **เป็นเรื่องแยกจากบั๊กภูมิประเทศ ห้ามปน**

---

## หนี้ทางเทคนิคที่รับไว้โดยตั้งใจ

1. **`var` ทั่วเอนจิน** คงไว้ตอนย้ายโดยตั้งใจ เปลี่ยนเป็น `const`/`let` ได้ทีหลัง
   ต้องรัน golden test คุมทุกครั้ง
2. **Leaflet ซ้ำซ้อนกับ MapLibre** MapLibre ทำ 2D ได้ ตัด Leaflet ออกได้ 62 จุด (สเปก N2)
   จะลด bundle ลงได้มากและเหลือ API แผนที่ชุดเดียว
3. **98 DOM id ผูกกับ HTML ตรงๆ** เปลี่ยนชื่อ id แล้วพังเงียบ ทำ map กลางทีหลังได้
4. **ไม่มี i18n** string ไทยกระจายอยู่ในโค้ด (สเปก N3)
5. **`app/src/app.js` ยังเป็นก้อนเดียว 1,586 บรรทัด** การแตกเป็น 7 ชั้นเป็นงานของแผน B
   ตอนนี้ยังไม่มี typecheck คุมไฟล์นี้เลย (`tsconfig.check.json` ไม่ครอบ `app/`)
6. **`lib: ES2020 + WebWorker` บังคับได้แค่ "รันในเวิร์กเกอร์ได้" ไม่ใช่ "ไม่ทำ I/O"**
   `fetch` `XMLHttpRequest` `importScripts` ยังเรียกได้จากในเอนจิน
   ให้ ESLint `no-restricted-globals` ในก้าว 4 คุมต่อ และควรจำกัด `self`/`postMessage`
   ให้ใช้ได้เฉพาะ `worker.ts`
7. **`doseGrid.truncated` ยัง `false` ทุกเคสใน golden** จะปลุกได้ต้องจัดฉาก fixture
   ซึ่งเป็นการปั้นเพื่อปลุกตัวชี้วัด ไม่ใช่เพื่อคุ้มกันโค้ด · `over`/`overMaxKm` มีชีวิตแล้ว
8. **`dist/worker.d.ts` ถูก emit แต่ไม่มีใครอ้าง** (`exports` ชี้ `src/worker.ts`) เป็น noise
9. **เวิร์กเกอร์รัน `src/*.ts` แต่ทางถอยรัน `dist/*.js`** — `app/src/app.js:5-6`
   `engineRunSync` มาจาก `'mokhwan-engine'` → `dist/index.js` ส่วน `EngineWorker`
   มาจาก `'mokhwan-engine/worker'` → `src/worker.ts` → `src/index.ts`
   ฟิสิกส์ชุดเดียวจึงถูกคอมไพล์สองทางในหน้าเดียว ถ้าใครแก้ `packages/engine/src/`
   แล้วไม่ build ทางเวิร์กเกอร์จะได้ของใหม่ ทางถอยได้ของเก่า และ golden test
   (ซึ่งอ่าน `src/`) เขียวทั้งสองแบบ · ระหว่างนี้ `npm run dev -w mokhwan-engine`
   (`vite build --watch`) ทำให้ `dist/` ตามทันได้
   **แก้ให้หายขาดคือทำ entry ของเวิร์กเกอร์ใน `dist/` แล้วชี้ `exports["./worker"]`
   ไปที่นั่น** ซึ่งจะทำให้ผู้ใช้ไม่ต้องมี bundler ที่แปลง TypeScript ด้วย (ดูข้อ 10)

10. **`mokhwan-engine/worker` ต้องมี bundler ที่แปลง TypeScript ได้** โหลดด้วย Node เปล่า
   หรือ Deno ที่ไม่ตั้งค่าจะไม่ทำงาน เขียนไว้ใน docstring ของ `worker.ts` แล้ว
   ควรย้ำใน README ตอนก้าว 4

---

## เรื่องที่ต้องหาคำตอบก่อนเริ่มก้าว 5

### ตัวเลขอ้างอิงใน HANDOFF ไม่ตรงกับที่เอนจินให้

`HANDOFF-terrain-mode.md` ระบุค่าอ้างอิงว่า เผาฟางข้าว 20 ไร่ 1 ชม.

| สภาพ | HANDOFF | ที่วัดได้จริง |
|---|---|---|
| เช้ามืด F ชั้นผสม 180 ม. | ~126 µg/m³ · ~13 กม. | **104.2** · 15.7 กม. (ถูกขอบโดเมนตัด) |
| กลางวัน B ชั้นผสม 1800 ม. | ~490 µg/m³ · ~2 กม. | **265.5** · 3.4 กม. |

`HANDOFF` ไม่ระบุความเร็วลม จึงกวาด `ws` 0.7–2.5 m/s ทดสอบสมมติฐาน
**พีคอิ่มตัวที่ 105 และ 266 ไม่มีทางถึงเป้า — `ws` อธิบายส่วนต่างไม่ได้**

ต้นเหตุที่ยังไม่ตรวจ: จำนวนไร่ · ความชื้นเชื้อเพลิง · `avg` · `res` · `rangeKm` ·
หรือค่าชุดนั้นมาจากเอนจินเวอร์ชันก่อนหน้า

**เกณฑ์รับงานของก้าว 5 อ้างตัวเลขชุดนี้** ถ้าไม่หาต้นเหตุก่อน จะไล่แก้บั๊กที่อาจไม่มีอยู่

### สคริปต์ DEM สังเคราะห์ใน HANDOFF ละพิกัดชดเชย

สคริปต์ใน `HANDOFF` (บรรทัด 169–178) เขียน `x = -R+(i+0.5)*cell` ตรงๆ ซึ่งถือว่า `cx=cy=0`
แต่ `runSim` เลื่อนศูนย์กลางกริดไปทางท้ายลม `0.32·R` เสมอ และ `makeSampler` อ่าน `elev`
ด้วยพิกัดที่ชดเชยแล้ว **ถ้าใช้สคริปต์นั้นตรงๆ ภูมิประเทศจะเลื่อนจากจุดเผาราว 2,263 ม.**
ทำให้จุดเผาไปอยู่ไหล่แอ่งตื้นแทนก้นแอ่ง — เคยพลาดตรงนี้มาแล้วใน Task 1
ดูตัวอย่างที่ถูกได้ใน `packages/engine/test/fixtures.ts` ฟังก์ชัน `syntheticDem`
