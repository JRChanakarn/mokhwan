# งานต่อ: โหมดตามภูมิประเทศจริงของ Smoke Plume Studio

## บริบท

ไฟล์ `smoke-plume-studio.html` เป็นแอปหน้าเดียว ไม่มี build step จำลองการฟุ้งกระจาย PM2.5
จากการเผาในที่โล่ง ใช้งานจริงในบริบทหมอกควันภาคเหนือของไทย เปิดผ่าน static server
(ต้องเป็น http ไม่ใช่ `file://` เพราะเรียก public API หลายตัว)

ตอนนี้ทำงานได้ครบแล้วในโหมดพื้นราบ งานที่เหลือคือ **โหมดตามภูมิประเทศจริง** ซึ่งเขียนโครง
ไว้แล้วแต่ยังมีบั๊กที่ทำให้ความเข้มข้นระดับพื้นออกมาเป็นศูนย์

---

## สถาปัตยกรรมที่มีอยู่ อย่าเปลี่ยนโดยไม่จำเป็น

ไฟล์เดียว แบ่งเป็นสามส่วน

1. `<script id="engine" type="text/plain">` — เอนจินฟิสิกส์ล้วน ไม่แตะ DOM เลย
   ตอนรันจะถูกโหลดเข้า Web Worker ผ่าน Blob URL ถ้าสร้าง Worker ไม่ได้จะ `eval`
   ลงมารันบน main thread แทน ส่งออกผ่าน `self.__ENGINE__ = {run, runPuff, windField, sigmas, plumeRise}`
2. `<script>` หลัก — state, Leaflet (2D), MapLibre GL (3D), UI, การเรียก API
3. CSS ในไฟล์ ธีมมืด ภาษาไทยทั้งหมด

**กติกาที่ต้องรักษา**

- เอนจินห้ามอ้างอิง `document`, `window`, หรือ Leaflet เด็ดขาด เพราะมันรันใน Worker
- การเปลี่ยนชั่วโมงหรือมุมมองต้องไม่คำนวณฟิสิกส์ใหม่ เอนจินคืนกริดของ **ทุกชั่วโมง**
  ในครั้งเดียว (`grids[]`, `maxGrid`, `doseGrid`) แล้ว `refresh()` แค่หยิบมาวาด
  เพราะปุ่ม play บนไทม์ไลน์ต้องเล่นได้ลื่น
- ทุกการเรียก API ภายนอกต้อง fail-safe ปิดตัวเองแล้วบอกเหตุผลเป็นภาษาไทย ห้ามพังทั้งแอป
- อย่าใช้ `element.style.display = ''` เพื่อ "เปิด" element ที่ CSS ตั้ง `display:none` ไว้
  มันคือการลบ inline style ทิ้งแล้วตกกลับไปใช้ค่าเดิม ต้องระบุ `'block'` ตรงๆ
  (บั๊กนี้เคยทำให้โหมด 3D เป็นจอดำมาแล้วรอบหนึ่ง)

**ค่าที่เอนจินคืนกลับมา** ทั้งสองโหมดต้องมีรูปร่างเดียวกัน

```
{ grids: Float32Array[],        // ความเข้มข้นระดับพื้นรายชั่วโมง µg/m³ (ยังไม่รวมพื้นหลัง)
  maxGrid, doseGrid,            // doseGrid หารด้วย 24 มาแล้ว = ค่าเฉลี่ย 24 ชม.
  N, cell, cx, cy, R,           // กริดเป็นสี่เหลี่ยมจัตุรัส แถว j=0 คือด้านเหนือ
  meanUx, meanUy, perHour[],
  recPerHour[][], recMax[], recDose[],
  totalEmitKg, totalFuelT, reqId }
```

---

## ฟิสิกส์ที่ใช้ อย่าเปลี่ยนค่าคงที่โดยไม่มีเหตุผล

- **Gaussian plume** + Briggs open-country sigma + Briggs buoyant plume rise
  + การสะท้อนที่พื้นและเพดานชั้นผสม
- แยกควันเป็นสองเฟส **เปลวไฟ** (ร้อน ลอยสูง) กับ **คุกรุ่น** (แรงยกตัวเหลือ 6%)
  สัดส่วนคุกรุ่นมาจากความชื้นเชื้อเพลิงและเพิ่มขึ้นตามเวลาที่ผ่านไปในการเผา
  ตรงนี้สำคัญมาก ผลกระทบระดับหายใจเกือบทั้งหมดมาจากเฟสคุกรุ่น
- `CONV = 0.35` สัดส่วนความร้อนที่ยกตัวเป็นพลูม, `HEAT = 18e6 J/kg`
- การปล่อยไม่คงที่ ชั่วโมงแรกหนักสุดแล้วลดลงตาม `exp(-1.6·p)`
- มีการตกสะสมแห้ง `vd = 0.004 m/s` และการชะด้วยฝน `Λ = 1e-4·P^0.7`
- แถบสี AQI อิงมาตรฐานไทย ค่าเฉลี่ย 24 ชม. 37.5 µg/m³

ตัวเลขที่ควรได้จากเคสอ้างอิง เผาฟางข้าว 20 ไร่ 1 ชั่วโมง

| สภาพ | พีคระดับพื้น | ระยะที่เกิน 37.5 |
|---|---|---|
| เช้ามืด F ชั้นผสม 180 ม. | ~126 µg/m³ | ~13 กม. |
| กลางวัน B ชั้นผสม 1800 ม. | ~490 µg/m³ | ~2 กม. |

ถ้าแก้อะไรแล้วตัวเลขเพี้ยนไปจากนี้มาก แปลว่าพัง

---

## งานที่ต้องทำ

### 1. แก้บั๊ก โหมด puff ให้ความเข้มข้นระดับพื้นเป็นศูนย์ ← เริ่มจากตรงนี้

`runPuff()` ในบล็อกเอนจิน เป็นแบบจำลองกลุ่มควันแบบ Lagrangian ปล่อย puff ทุก 60 วินาที
แล้วพัดไปตามสนามลมที่คำนวณจาก DEM

**สถานะที่ยืนยันแล้วด้วยการรันจริง**

```
พื้นราบ (elev = null):  puffs=120  maxG=71.8  ff=1.00  puff0 d=4680m z=110 zg=0
ภูมิประเทศ (elev = Z):  puffs=120  maxG=0.000 ff=0.48  puff0 d=768m  z=289 zg=183
```

- โหมดพื้นราบ **ทำงานถูกต้อง** ให้ผลตรงกับ Gaussian
- สนามลม `windField()` **ทำงานถูกต้องแล้ว** หลังแก้รอบล่าสุด ความเร็วอยู่ในช่วง
  0.54–1.30 m/s เมื่อลมตั้งต้น 1.30 ไม่มีการพลิกทิศ ลมเบนขนานเส้นชั้นความสูงตามที่ควร
  (รอบก่อนหน้าเคยพลิกทิศเพราะใช้วิธี **หักลบ** เวกเตอร์ ตอนนี้เปลี่ยนเป็น **หมุนทิศโดยรักษาความเร็ว** แล้ว)
- puff เดินทางแค่ 768 ม. ในหนึ่งชั่วโมง **อันนี้ถูกต้องตามที่ออกแบบ** เพราะลมไหลลงลาด
  บรรจบกันที่ก้นแอ่ง เกิดจุดชะงัก นี่คือพฤติกรรม "กระจุกในแอ่ง" ที่ต้องการ
- แต่ `maxG` ออกมา 0.000 ซึ่งผิด

**สมมติฐานที่ยังไม่ได้พิสูจน์**

เฟสเปลวไฟลอยอยู่ที่ agl ประมาณ 106 ม. และ σz ที่ระยะ 768 ม. ในความเสถียร F มีค่าราว 10 ม.
ทำให้ `exp(-0.5·(106/10)²) ≈ 0` อันนี้ถูกต้องตามฟิสิกส์ ควันยังไม่มุดลงถึงพื้น

ปัญหาคือ **เฟสคุกรุ่น** ที่ agl ประมาณ 40 ม. ควรให้ค่าราวหลายหน่วย µg/m³
แต่กลับหายไปด้วย ต้องหาว่าหายตรงไหน จุดที่น่าสงสัย

- ค่า cutoff `if(norm > 1e-4)` และ `if(val < 1e-4) continue` อาจตัดทิ้งเร็วเกินไป
  เมื่อคูณด้วย `invHour = DT/dt = 1/60` แล้ว
- normalization ของ puff `m/((2π)^1.5 · σy² · σz)` ตรวจมิติให้แน่ใจ
  โดยเฉพาะการคูณ `1e6`, `C.tf`, และ `invHour` ว่าซ้อนกันถูก
- `srcs[]` สร้างจาก `C.groups[].layers[]` โดย `q: Ly.q * grp.pts.length`
  ตรวจว่าเฟสคุกรุ่นถูกใส่เข้าไปจริง (`srcs.length` ควรเป็น 2 ต่อหนึ่งแปลง)
- `agl = Math.max(2, Math.min(pf.z - zg, L*1.05))` ตรวจว่า `pf.z` ตั้งต้นถูก
  ควรเป็น `terrain_at_source + plumeRise` ไม่ใช่ `plumeRise` เฉยๆ

**เขียน unit test ก่อนแก้** เทียบ puff กับ Gaussian บนพื้นราบสมบูรณ์ ผลต้องต่างกันไม่เกิน 25%
ที่ระยะ 1, 3 และ 8 กม. ถ้า test นี้ผ่านแล้วค่อยเปิด DEM

### 2. ดึง DEM จริง ยังไม่ได้ทำเลย

เอนจินรับ `P.elev` เป็น `Float32Array` ขนาด N×N หน่วยเมตร เรียงตรงกับกริดความเข้มข้น
ฝั่ง main ต้องไปหามาให้

- **แหล่งหลัก** AWS Terrain Tiles รูปแบบ terrarium
  `https://s3.amazonaws.com/elevation-tiles-prod/terrarium/{z}/{x}/{y}.png`
  ถอดค่าด้วย `elev = (R*256 + G + B/256) - 32768`
  ต้องตั้ง `img.crossOrigin = 'anonymous'` ก่อนวาดลง canvas ไม่งั้น `getImageData` จะโดนบล็อก
  เลือก zoom ให้โดเมนครอบด้วยไทล์ราว 4–9 ใบ
- **แหล่งสำรอง** Open-Meteo Elevation API
  `https://api.open-meteo.com/v1/elevation?latitude=a,b,c&longitude=x,y,z`
  ได้สูงสุด 100 จุดต่อคำขอ ใช้กริด 20×20 แล้ว bilinear upsample ความละเอียดหยาบกว่า
  แต่พอสำหรับผลระดับแอ่ง และ CORS ใช้ได้แน่นอนเพราะเป็น provider เดียวกับพยากรณ์อากาศ
- cache ตาม key ของโดเมน อย่าดึงซ้ำทุกครั้งที่ขยับพารามิเตอร์

### 3. แสดงภูมิประเทศในโหมด 2D

ผู้ใช้ขอมาโดยตรง ต้องเห็นว่าแอ่งกับสันเขาอยู่ตรงไหนโดยไม่ต้องสลับไป 3D

- คำนวณ hillshade จาก DEM ลง canvas แล้ววาง `L.imageOverlay` ไว้ **ใต้** ชั้นควัน
- เพิ่มเส้นชั้นความสูงด้วย `d3.contours` (d3 โหลดอยู่แล้ว) สัก 6–8 ระดับ สีจางๆ
- ทางเลือกที่ดี วาดลูกศรสนามลมที่ถูกภูมิประเทศเบนแล้วทับลงไป จะเห็นเลยว่าลมเลี้ยวตรงไหน
  โค้ดวาดอนุภาคลมมีอยู่แล้วใน `startWind()` ต่อยอดจากตรงนั้นได้

### 4. ต่อ UI

- ตัวเลือกแบบจำลองในแผงซ้าย **พื้นราบ (Gaussian) เร็ว** กับ **ตามภูมิประเทศ (Puff) ช้ากว่า**
  ส่งไปเป็น `payload.model = 'gauss' | 'puff'`
- แสดงสถานะ DEM ให้เห็น ความละเอียดกี่เมตร ความต่างระดับในโดเมนกี่เมตร ค่า Froude
  ของแต่ละชั่วโมง (เอนจินคืน `perHour[].Fr` และ `.relief` มาให้แล้ว)
- โหมด puff ใช้เวลานานกว่า ต้องมี progress ไม่ใช่ค้างเงียบ
  ให้ worker `postMessage` ความคืบหน้ารายชั่วโมงกลับมา

---

## วิธีทดสอบ ใช้ชุดนี้ได้เลย ประหยัดเวลามาก

**ทดสอบเอนจินใน node โดยไม่ต้องเปิดเบราว์เซอร์** ดึงบล็อกเอนจินออกมาแล้ว require

```js
// สกัดเอนจินออกจาก HTML
const s = fs.readFileSync('smoke-plume-studio.html', 'utf8');
const eng = s.match(/<script id="engine" type="text\/plain">([\s\S]*?)<\/script>/)[1];
fs.writeFileSync('eng.js', eng);

global.self = global;
require('./eng.js');
const E = global.__ENGINE__;
const r = E.run({ model:'puff', fires:[...], hours:[...], elev: Z, ... });
```

**ภูมิประเทศสังเคราะห์สำหรับทดสอบ** แอ่งตรงกลาง สันเขาขวางทางตะวันตกเฉียงใต้
ตั้งลมมาจาก 45° แล้วเช็กสามอย่าง ควันต้องกระจุกในแอ่ง ต้องเลี้ยวเลาะสันเขา
และต้องข้ามไปอีกฝั่งสันเขาน้อยกว่าโหมดพื้นราบอย่างมีนัยสำคัญ

```js
const Z = new Float32Array(N*N);
for (let j=0; j<N; j++) for (let i=0; i<N; i++) {
  const x = -R+(i+0.5)*cell, y = R-(j+0.5)*cell;
  Z[j*N+i] = Math.max(0,
      300
    + 620*Math.exp(-Math.pow((x*0.7071 + y*0.7071 + 3500)/1800, 2))   // สันเขา
    - 140*Math.exp(-(x*x + y*y)/(2*2600*2600)));                       // แอ่ง
}
```

**ทดสอบ UI ด้วย playwright-core** เรียก state ตรงๆ ได้เลยเพราะ `const` ระดับบนสุด
อยู่ใน global lexical environment

```js
await page.evaluate(() => {
  setWxMode('man');
  S.man = {ws:1.4, wdir:35, stab:'F', mix:200};
  S.dur = 5; syncAllInputs();
  addPlot({type:'point', latlng: L.latLng(18.7883, 98.9853), rai:40});
});
```

ถ้าทดสอบแบบ offline ให้ vendor `leaflet`, `maplibre-gl`, `d3` จาก npm มาไว้ในโฟลเดอร์
แล้วแทน URL ของ cdnjs ชั่วคราว

---

## เกณฑ์ว่าเสร็จ

1. โหมด puff บนพื้นราบให้ผลใกล้ Gaussian ต่างกันไม่เกิน 25% ที่ 1, 3, 8 กม.
2. เปิด DEM แล้วเคสภูมิประเทศสังเคราะห์ให้ควันกระจุกในแอ่งมากกว่าพื้นราบชัดเจน
   และข้ามสันเขาไปน้อยกว่าชัดเจน
3. เผาจริงในแอ่งเชียงใหม่ตอนเช้ามืด ควันต้องไม่ปีนข้ามดอยสุเทพ แต่เลาะไปตามหุบ
4. โหมด 2D เห็นภูมิประเทศพร้อมชั้นควัน
5. ไทม์ไลน์ยังกด play ได้ลื่นเหมือนเดิม การเปลี่ยนชั่วโมงห้ามคำนวณใหม่
6. ทุก API ที่ล่มต้องไม่ทำให้แอปพัง

---

## ข้อจำกัดที่ต้องบอกผู้ใช้ในแอป อย่าขายเกินจริง

สนามลมเป็นแบบ diagnostic สองมิติ ไม่ได้แก้สมการพลศาสตร์ของไหล มันจับได้แค่การเบนรอบภูเขา
ลมไหลลงลาด และการอับลมในแอ่ง แต่จับไม่ได้เรื่องคลื่นภูเขา การแยกตัวของกระแสหลังสันเขา
และการสะสมข้ามวันใต้อินเวอร์ชัน ระดับที่ใช้ตัดสินใจเชิงนโยบายจริงต้องใช้ CALPUFF หรือ WRF-Chem
เครื่องมือนี้เหมาะกับการเปรียบเทียบทางเลือกและสื่อสารให้เห็นภาพ ไม่ใช่ค่าตรวจวัด
